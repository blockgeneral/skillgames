/** Simulated opponent configuration for single-player prototype only */
export const SIMULATED_OPPONENT = {
  MIN_REACTION_MS: 350,
  MAX_REACTION_MS: 500,
  HIT_RATE: 0.9,
  FALSE_START_RATE: 0.03,
} as const;
